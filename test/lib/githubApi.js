describe('Routing', function() {
	it('it should return error "github is unavaliable" and log the error, trying to get github API that returns an error.', function() {
	});

	it('it should log error when trying send response for invalid "res" object', function() {
	});

	it('it should send the body object of a valid response to "github/status"', function() {
	});
};